import React from "react"
import ReactDOM from "react-dom/client"
import CajeroAutomatico from "../src/components/cajero-automatico"
import "./index.css"


ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CajeroAutomatico />
  </React.StrictMode>,
)
